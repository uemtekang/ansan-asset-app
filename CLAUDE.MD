# ansan-asset-app 개발 지침서

## 프로젝트 개요
이 프로젝트는 안산영업소 창고 자산 관리 앱이다.

기술 스택:
- React
- Vite
- Supabase
- Vercel

목표:
- 자산 등록
- 자산 상태 관리
- 검색/목록 조회
- 대시보드 상태별 통계
- 대시보드 카드 클릭 시 상태별 목록 이동
- 추후 사진 업로드 및 표시 기능 추가

---

## 가장 중요한 원칙
1. 기존 동작하는 기능을 최대한 유지할 것
2. 요청한 기능과 관련 없는 부분은 수정하지 말 것
3. 한 번에 대규모 리팩토링하지 말 것
4. 현재 파일 구조와 라우팅 구조를 먼저 확인한 뒤 수정할 것
5. 새 파일 추가보다 기존 파일 재사용을 우선할 것
6. UI는 현재 안산영업소 창고 자산 앱 분위기를 유지할 것
7. 수정 후 import/export 누락이 없게 할 것
8. 복붙 가능한 전체 코드 또는 정확한 수정 블록으로 답할 것

---

## 작업 순서
Claude는 항상 아래 순서로 작업할 것.

### 1. 현재 구조 해석
먼저 아래를 파악해서 요약할 것.
- 페이지 파일 구조
- 대시보드 파일 위치
- 검색/목록 파일 위치
- Supabase client 파일 위치
- React Router 구조
- 현재 status 컬럼명 및 실제 상태값

### 2. 수정 계획 제시
코드를 쓰기 전에 먼저 아래를 설명할 것.
- 어떤 파일을 수정하는지
- 왜 수정하는지
- 어떤 방식으로 동작하게 할지
- 기존 기능에 영향이 있는지

### 3. 최소 수정 원칙으로 구현
- 가능한 기존 검색/목록 페이지를 재사용할 것
- 상태별 페이지를 여러 개 새로 만들기보다 필터 방식으로 처리할 것
- 불필요한 라이브러리 추가 금지

### 4. 코드 제공 방식
- 파일명 기준으로 정리할 것
- import 포함
- 복붙 가능한 전체 코드 또는 정확한 수정 블록으로 작성할 것
- 생략 부호(...) 없이 줄 것

### 5. 적용 후 확인 방법 제시
마지막에 반드시 아래를 포함할 것.
- 어디를 클릭해야 하는지
- 어떤 결과가 떠야 하는지
- 에러가 나면 어디를 확인해야 하는지
- Supabase 컬럼 확인이 필요한지

---

## 금지 사항
다음은 하지 말 것.
- 요청하지 않은 라이브러리 추가
- TypeScript로 전환
- 폴더 구조 전면 개편
- 기존 파일명 대규모 변경
- 기존 DB 컬럼명 임의 변경
- 존재하지 않는 파일을 있다고 가정
- Vite 프로젝트를 Next.js 방식으로 작성
- 확인 없이 React Router 코드를 추측 작성
- 전체 앱을 갈아엎는 방식 제안

---

## Supabase 규칙
1. DB는 Supabase를 사용한다
2. 기존 supabase client 설정을 재사용할 것
3. 기존 환경변수 이름을 유지할 것
4. status 필터는 실제 DB 컬럼명 기준으로 맞출 것
5. 사진 기능은 추후 Supabase Storage를 사용할 예정
6. image_url 컬럼이 없으면 먼저 필요 여부를 설명할 것
7. 쿼리 작성 시 에러 처리와 null 처리 포함할 것

예시 원칙:
- status 값이 없으면 전체 조회
- status 값이 있으면 `.eq('status', status)` 적용
- 에러 발생 시 console.error 또는 사용자 메시지 처리

---

## UI/UX 규칙
1. 대시보드 카드 디자인은 최대한 유지할 것
2. 클릭 기능 추가 시 hover 효과와 cursor pointer 정도만 추가할 것
3. 모바일에서도 레이아웃이 깨지지 않게 할 것
4. 데이터가 없으면 빈 상태 메시지를 보여줄 것

예시:
- 조회된 자산이 없습니다
- 보관중 자산이 없습니다

---

## 라우팅 규칙
가능하면 아래 구조를 우선 검토할 것.
- 전체: `/search`
- 상태별: `/search?status=보관중`
- 또는 현재 목록 페이지 route 재사용

원칙:
- 페이지를 여러 개 새로 만들기보다
- 기존 목록 페이지 하나로 상태 필터 처리

---

## 상태값 규칙
현재 대시보드 상태값:
- 전체
- 보관중
- 사용중
- 고장
- 불명

Claude는 DB에 저장된 실제 값과 UI 표시값이 일치하는지 먼저 확인할 것.
추측하지 말고 현재 코드와 데이터 기준으로 맞출 것.

---

## 응답 형식 규칙
항상 아래 형식으로 답할 것.

### 1. 현재 구조 해석
- 현재 파일 구조 요약
- 현재 동작 방식 요약

### 2. 수정 계획
- 수정할 파일
- 수정 이유
- 동작 방식

### 3. 수정 코드
- 파일명 기준
- import 포함
- 복붙 가능한 코드

### 4. 적용 후 확인 방법
- 클릭 위치
- 기대 결과
- 에러 확인 위치

### 5. 추가로 필요한 DB 작업
- 컬럼 추가 필요 여부
- Storage 버킷 필요 여부

---

## 현재 우선 구현 기능
1. 대시보드의 전체/보관중/사용중/고장/불명 카드를 클릭 가능하게 만들기
2. 클릭 시 해당 상태의 목록 화면으로 이동
3. 목록 화면은 기존 검색/목록 페이지를 재사용
4. Supabase에서 status 조건으로 필터 조회
5. 추후 보관중 목록에서 사진까지 표시
6. 사진은 Supabase Storage + image_url 컬럼 사용 예정

---

## 최종 원칙
이 프로젝트는 새로 만드는 것보다
기존 동작 앱을 안전하게 개선하는 것이 더 중요하다.

그러므로 Claude는
- 작고 확실한 수정
- 안전한 수정
- 복붙 가능한 수정
- 에러 가능성이 낮은 수정
위주로 작업할 것.